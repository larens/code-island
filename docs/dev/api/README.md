# API / 接口文档

API 文档、接口约定和协议规范。

---

## English

Store API documentation, hook protocol specifications, and integration contracts here. For Code Island, this primarily covers the Claude/Codex hook payload formats, the CodeIslandBridge socket protocol, and any external API surfaces. Use DocC or Swagger if the API grows complex.

## 中文

在此存放 API 文档、hook 协议规格和集成契约。对于 Code Island，主要包括 Claude/Codex hook payload 格式、CodeIslandBridge socket 协议和任何外部 API 接口。如 API 较为复杂，可使用 DocC 或 Swagger。

---

**Note:** For the hook protocol spec, also see `../spec/` for behavioral requirements and `architecture/` for system-level diagrams.
